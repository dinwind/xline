# Release 0.3.3 — Feedback Navbar fix

- Root cause: Navbar lived inside ChatView (`display:none` when Settings open); Feedback entry was easy to miss / not a stable chrome item. 0.3.1 bundle already contained `id:"feedback"` but UX overlay stacking made it unreliable.
- Fix: App-level Navbar; `codicon-feedback`; overlays `absolute` under chrome; `.vscodeignore` dist whitelist.
- 0.3.2 published but oversized (inspect artifacts) — superseded by 0.3.3.
- AuthNexus: releaseId `4a2f3dde-71ee-40db-ac7c-79c7c19a6768`, version `0.3.3`, PUBLISHED, verify OK.
