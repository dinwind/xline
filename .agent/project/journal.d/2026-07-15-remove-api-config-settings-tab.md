# Remove API Configuration settings tab

- Removed `api-config` tab from `SettingsView` (Axline no longer exposes Cline BYOK API settings UI).
- Default settings tab is now **Features**.
- Model/API banner actions and chat model button navigate to **Account** instead of API Configuration.
